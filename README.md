# 8bitHome Streamlit homepage

A simple Streamlit homepage with a sidebar that lets you switch between a homepage and a GDP dashboard.

### How to run it on your own machine

1. Install the requirements

   ```
   $ pip install -r requirements.txt
   ```

2. Run the app

   ```
   $ streamlit run streamlit_app.py
   ```
